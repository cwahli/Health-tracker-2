# Archived golden promotions

Demoted from the official `G1`–`G7` contract (2026-08-20).

These were inbox promotions with **no photos**, duplicate jobs, or transport-only tapes (`bug`, `Try golden`, quota, duplicate wraps). Layer B `resolveLocks` here are not the product contract.

Official set: `tests/Golden_meal/1.` … `7.` plus `manifest.json`.
Do not re-add these to `manifest.json` to paint CI green.
