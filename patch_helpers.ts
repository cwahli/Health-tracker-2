import fs from 'fs';
let code = fs.readFileSync('server.ts', 'utf8');

// I messed up moving it. Let's get the original code back.
// Since there's no git, I'll need to manually grab it from the earlier version, wait, I can just write it again.
