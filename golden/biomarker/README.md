# golden/biomarker — biomarker benchmark goldens

New biomarker benchmark cases land here, mirroring the `golden/meal/`
contract (fixture + instruction + expected).

The canonical executed suite today is `tests/Golden_biomarker/` (run via its
existing gates). Do not duplicate those fixtures here — reference them.
