# AI_HANDOVER

## Status
- **2026-09-12 Cross-Device Image Sync & Direct R2 S3 Upload FIX**: User reported meal images weren't shared across devices (missing hero image, composition crops falling back to Unsplash stock photos on Device B). Root Cause: (1) Server code (`serverJobs.ts`, `server_routes_jobs.ts`, `server_routes_sync.ts`) was importing `uploadPhotoToR2` from `src/utils/r2Storage.ts` which called relative `fetch('/api/upload')`, failing in Node.js with `TypeError: Failed to parse URL`. (2) `r2Storage.ts` error handler caught this and returned the `jobId` string argument, persisting literal `job_...` string as `photoUrl` in database/memory instead of an image URL. Device A showed the image solely via local IndexedDB; Device B rejected the non-URL `jobId`, received 0 usable images, and hid the hero slider. (3) `*.r2.dev` is blocked by Indonesian ISPs, making same-origin `/photos/:key` proxy streaming essential. Fix: (1) In `server_routes_r2.ts`, exported `uploadPhotosToR2Direct`, `uploadDebugPayloadToR2Direct`, `uploadLogsToR2Direct`, `fetchLogsFromR2Direct`, and `fetchDebugPayloadFromR2Direct` using `@aws-sdk/client-s3` directly with zero loopback HTTP; added `/api/debug/load` and `/api/r2/logs` endpoints; aliased `POST /api/upload` to `/api/r2/upload-photo` supporting both `{ jobId, payload }` and `{ data, prefix, filename }`. (2) In `serverJobs.ts`, switched photo, debug, and log uploads to direct S3 functions; added `isValidPhotoUrl` validator rejecting raw `jobId` strings. (3) In `src/utils/r2Storage.ts`, fixed client upload endpoints to `/api/r2/upload-photo`, fixed fallback to never return `jobId` strings, and added `userId` parameter to `fetchDebugPayloadFromR2`. (4) In `src/components/LogChat.tsx` and `src/components/chat-cards/FoodCard.tsx`, filtered out `msg.id` and raw job ID strings from image arrays, added `/photos/${jobId}.jpg` proxy fallback, preserved hook order above boundary. Gates: `FoodCard.hooks.test.ts` (2/2), `foodImageSources.test.ts` (9/9), `JobSession.contract.test.ts` (10/10), `mergeFoodEditMessages.test.ts` (3/3), `firestoreUtils.test.ts` (6/6), `npm run build:web` exit 0, `node scripts/journey-guard.mjs` PASS.
- **2026-09-12 Mode D Comparison Grouping Template in Chat Modal FIX**: User reported that in the chat modal for Compare/Shelf Evaluation runs, the dedicated comparison template and tiered groupings (safe/warning/alert) were not being rendered, falling back to a raw meal composition list. Fixed: (1) In `FoodCard.tsx`, generalized mode checks from strict `mode === 'evaluation'` to `isCompareEvaluation` recognizing `mode === 'compare'`, `job.kind === 'food_compare'`, and checking top-level `groups` / `rawScout.groups`; wired `FoodEvaluationComparisonCard` fallback for option sets; preserved hook order above boundary. (2) In `LogChat.tsx`, updated assistant polling updater to avoid skipping compare runs where `pendingFoodLog` is null, carried comparison and mode in payload. (3) In `App.tsx`, preserved mode and comparison payloads in poller message dispatcher. (4) Updated `compareMealLogGuard.ts` and unit tests. Gates: `FoodCard.hooks.test.ts` (2/2), `compareMealLogGuard.test.ts` (3/3), `JobSession.contract.test.ts` (10/10), `ModeDAndEdit.test.ts` (2/2), `npm run build:web` exit 0, `node scripts/journey-guard.mjs` PASS.
- **2026-09-12 R-13.3 Built-in Email Auth & Historical Data Continuity COMPLETE**: Locked spec `specs/active/R-13.md`. Added `app_users` table in `server_d1_schema.ts` and live-migrated Cloudflare D1. Implemented scrypt password hashing + HMAC session tokens (`htk.*`) in `server_auth.ts`. Pre-configured Admin `cwah.liu@gmail.com` mapped directly to historical UID `hiJun2hTdDTk2igwerun2LKvwb42` in D1 (preserving all 150 food logs and 35 biomarker logs). Seamless token injection via `breadcrumbTracker.ts` customFetch without touching frozen files. Wired `AuthScreen.tsx` manual login/signup. Gates: `server_auth.test.ts` (9/9), spec-diff PASS, egress-bomb PASS, `build:web` + `build:server` green, Guard PASS. Next: Cloudflare Pages git deploy & backend container deploy.
- **2026-09-12 R-13.1 Cloudflare go-live SHIP COMPLETE**: Locked spec `specs/active/R-13.md`. Split build scripts into `build:web` (Vite SPA) and `build:server` (Node bundle), parameterized `PORT` (default 3000) and `INTERNAL_BASE_URL` in `server.ts`. Created `Dockerfile` and `.dockerignore` for Cloudflare Container backend, and `public/_redirects` / `wrangler.jsonc` for Cloudflare Pages SPA client routing. AI Studio `npm run dev` unchanged (`tsx server.ts` port 3000, Vite). Gates: handoffContract + server_derivation + server_sse_json (47/47) pass, `build:web` + `build:server` green, assert-spec-diff PASS, Guard PASS; `tsc` 31 = 31 baseline. Next: Cloudflare dashboard connect & deploy (R-13.0 credentials + R-13.1 container run).
- **F-8.10 Goldilocks Split COMPLETE**: Refactored `server_food_analyze_run.ts` from 1,537 lines (and previously 3,560 lines) into focused, modular owners (orchestrator: 261 lines; scout: 117 lines; precalc: 240 lines; dietitian/narration: 337 lines; finalize/responses: 562 lines). Deleted unreferenced dead backup `server_food_analyze_run_original.ts`. All standing invariants (`docs/agent/standing.json`), fingerprint checks (`journeyFingerprints.test.ts`), single-path tests (`server_food_analyze_single_path.test.ts`), and Playwright shell smoke suites passing exit 0.
- **R-8 Client TTI & Baseline Measurement COMPLETE**: Measured navigation, paint, and network request counts across Home, Health, and Chat surfaces. Baseline Home metrics: DOMContentLoaded = 1,485ms, Load = 2,223ms, FCP = 1,644ms, DOMInteractive = 136ms with 203 initial HTTP requests (including unbundled dev assets) and duplicate `/api/jobs/status` startup fetches. Health tab transition: 8 requests. Chat quick-action drawer: 0 requests.
- **Dietitian Deprecation & Cleanup COMPLETE**: Purged dead fallback LLM evaluation branch from `server_food_analyze_run_dietitian.ts`, making the phase a deterministic TypeScript projector/narrator with zero secondary LLM calls. Renamed execution phase to `executeMealProjectorPhase`. Updated user-facing labels in `translations.ts` (en and id), `FullScreenInstructionViewer.tsx`, `FullScreenLogViewer.tsx`, `GoldenInboxPanel.tsx`, and `FoodCard.tsx` from "Dietitian" to "Meal Agent" while maintaining backward compatibility for stored log fields (`dietitianScratchpad`, `dietitianAnswer`). Guard, tsc, and all regression suites passing exit 0.
- **Next**: Wire Unified Biomarker Agent into Production (C1–C7 Gate Cleared) or tackle front desk routing.
- **2026-09-11 roadmap pass (F-4 + F-3 + F-8.12 COMPLETE)**: F-4 measured alias hit rate (`computeAliasHitRate`) + `NEAR_DUP_CLUSTER` merge gate in `server_food_catalog.ts`; F-3 FALSE_FRIEND `PLANT_MILK_AS_DAIRY` hard reject (oat/soy/almond/coconut milk never resolve to dairy `746782`) in `server_food_db.ts`; F-8.12 real Hemaviton C1000 brand row in `brand_menu_items_local.json` + micro passthrough (`vitaminC` etc.) in `server_brand_match.ts`. Gates: `tsc` 0, 171 food tests pass, Guard/M31/ingest/f10-pr1 PASS. Residuals for human/Grok: B0 Apply smoke (human), live C1–C7 re-run before modal wiring, B7.4/B7.6 live-profile sweeps, Q-8.6/F-10.8 outer soak, Q-1 god-file ceilings red on `App.tsx`/`LogChat.tsx`/`server_vision_scout.ts` (Grok Q-9).
- **2026-09-12 FoodCard hooks crash FIX**: "View Analysis" crashed with "Rendered fewer hooks than expected" — `FoodCard` returned the `msg.isLive` progress UI before its ~30 hooks, so a live→resolved message re-rendered the same instance with a different hook count. Moved the live branch + non-food null guard below the last hook (`__FOODCARD_HOOKS_END__`); sensor `FoodCard.hooks.test.ts` locks the ordering. No new `tsc` errors (HEAD `8b30d87` already red pre-existing); dialog-inventory 2 Playwright fails identical on baseline. Residual warnings (not crash): NaN `children`, setState-in-render `FoodHistoryTab`/`LogChat`, sw.js `/` fetch miss.
- **2026-09-12 portion-confirm setState-in-render FIX**: `LogChat` PortionClarifyCard `onConfirm` called `persistAnswered` (`JobStore.updateJob` → notify → `FoodHistoryTab` setState) INSIDE `setMessages(prev => …)` updaters — updaters run during render, hence the warning. Hoisted to compute-then-persist-then-set in both local (≤30%) and agent-edit (>30%) paths (`LogChat.tsx`); sensor `LogChat.portionConfirm.test.ts` scans all updaters for store writes. Behavior unchanged: ≤30% still recalcs locally with no agent call (by design — `applyPortionChoicesToLog`), >30% still `handleSend`s an edit. `tsc` 31 = 31 baseline (zero new); Guard FAIL is the pre-existing shell-smoke 2 Playwright fails (identical on stash baseline).
- **2026-09-12 orphaned hero image FIX (all 3)**: (1) `ImageSlider` returns null when the list dedupes to zero (was a placeholder box leaking raw key `imgNoImages`); `FoodCard` hero now guards on the `uniqueMealImageUrls`-deduped list so hero/tiles agree. (2) `imgNoImages` key ref removed with the placeholder — no translations change needed. (3) Save-time sanitize in `TaskPlaceholderCard.handleLocalSave`: `uniqueMealImageUrls` replaces the `normalizeMealImageUrl(r) || r` fallback that persisted dead strings; final list re-sanitized (durable preferred, `data:` kept offline) before persist. Sensors: `ImageSlider.empty.test.tsx` (3/3) + `uniqueMealImageUrls` all-dead→`[]` case. `tsc` 31 = 31 baseline, zero in touched files. Residual: other missing `img*` keys (`imgUnavailable/imgPrev/imgNext/imgGoTo/imgClose`) still leak raw keys on broken-image/viewer paths — pre-existing S-1 leftover-chrome, not touched.
- **2026-09-12 narrator removed repo-wide (Scout-only)**: Removed synthetic narrator dispatch and backfill repo-wide. Scout (`t1/scout`) directly carries `output` and `rawEmission` (verdict + clinical advice) self-covering the verdict+advice contract law without synthetic rows. Purged `composeNote`, `buildNarratorDispatch`, `PROJECTOR_NARRATOR_INSTRUCTION`, and export backfill in `debugRunTree.ts`. Updated fixtures and tests in `dumpContract.test.ts`, `server_food_dietitian_dispatch.test.ts`, and added negative assertions in `server_food_analyze_single_path.test.ts`. Backward-compatible `dietitian_answer` streaming log tags preserved for `LogChat`/`FoodCardViewers` UI continuity. Gates: food-calc 133 tests, single-path, dumpContract, debugRunTree, shell-smoke (8/8) PASS, Guard PASS; `tsc` 31 = 31 baseline.
- **2026-09-12 S-10 PORTION_FUNNEL COMPLETE**: picker asked redundantly on stated quantities, silent on pack divergence. `quantityText.ts` locale boundary + candidate funnel (`user > label > visual`) in `server_portion_clarify.ts` + precalc threading of `ctx.message` + shared text blob + per-option sanity + scout dispatch leg + `quantityResolution` debug record. No prompt/schema/ledger changes. Gates: portion_clarify 28 + quantityText 10 + portion-funnel Playwright + shell-smoke 8/8 + Guard PASS; `tsc` 31 = 31 baseline. Residuals: dispatch/narration i18n leaks, `golden_meals.test.ts` missing-fixture ENOENT (both pre-existing, baseline-identical).

## Notes
- **Scout is the sole Meal Agent (Dietitian fully deprecated)**: All meal extraction, nutrient estimates, and clinical coaching advice are handled exclusively by Scout (Meal Agent). The clinical feedback loop is two-sided: balancing alerts on threshold excesses (sodium, saturated fat) with praise for positive nutrient achievements (protein, soluble fiber, healthy fats).
