import { PipelineRunContext } from './food_analyze_run_types.js';

export async function processFoodAnalyze(ctx: PipelineRunContext) {
    // We will extract blocks from server_food_analyze_run.ts into here
}
