---
description: Always-on for Antigravity. Same journey graph as AGENTS.md. Do not skip go.
---

On “review”, “improve”, or a journey: follow `docs/agent/JOURNEY.md`. Write `specs/active/<slug>.md` as **draft**. Do not edit `src/` until the human says **go** (packet `status: locked`). Then `node scripts/journey-guard.mjs`. Skills: planner → guard → builder → guard. Reviewer only after a second Guard fail or “what went wrong”.
